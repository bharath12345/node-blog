div.post
  h3
    a(href=post.url)
      = post.title
  section.byline #{post.date.getDate()}.#{post.date.getMonth()+1}.#{post.date.getFullYear()}
  div.post.content
    != post.preview
  div.tags
    - each tag in post.tags
      a(href=tagUrl(tag))
        span.label.label-info #{tag}
